# ─── Core Web Framework ────────────────────────────────────────────────
Django>=5.0,<5.2
djangorestframework>=3.15.2
django-cors-headers>=4.4.0
django-environ>=0.11.2
django-filter>=24.3

# ─── Authentication (JWT + Google OAuth via id_token verification) ────
djangorestframework-simplejwt>=5.3.1

# ─── Database ──────────────────────────────────────────────────────────
# mysqlclient ships pre-built wheels on Windows for CPython 3.12.
# If the wheel is unavailable, swap to `PyMySQL` and add a shim in manage.py.
mysqlclient>=2.2.4

# ─── Caching / Messaging ───────────────────────────────────────────────
redis>=5.0.7
django-redis>=5.4.0
kafka-python>=2.0.2

# ─── Production Server ────────────────────────────────────────────────
gunicorn>=22.0.0

# ─── Media / Imaging ──────────────────────────────────────────────────
pillow>=10.4.0
requests>=2.31.0

# ─── PDF Generation ──────────────────────────────────────────────────
# ReportLab is the cross-platform choice (no GTK / Cairo needed on Windows).
reportlab>=4.0.9

# ─── AI / RAG Stack ───────────────────────────────────────────────────
# Chat/generate still uses `google-generativeai`; embeddings use `google-genai`.
google-generativeai>=0.7.2
google-genai>=1.0
chromadb>=0.5.5

# ─── ML — Sentiment + Vision + Forecasting ────────────────────────────
# torch wheels from PyPI default to CPU on Windows (no CUDA). On Linux/macOS
# install via the official PyTorch CPU index for the smallest footprint:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.2
torchvision>=0.17.2
transformers>=4.43.3
prophet>=1.1.5
