# ─── Core Web Framework ────────────────────────────────────────────────
Django>=5.0,<5.2
djangorestframework>=3.15.2
django-cors-headers>=4.4.0
django-environ>=0.11.2
django-filter>=24.3

# ─── Authentication (JWT + Google OAuth via id_token verification) ────
djangorestframework-simplejwt>=5.3.1

# ─── Database ──────────────────────────────────────────────────────────
# mysqlclient ships pre-built wheels on Windows for CPython 3.12.
# If the wheel is unavailable, swap to `PyMySQL` and add a shim in manage.py.
mysqlclient>=2.2.4

# ─── Caching / Messaging ───────────────────────────────────────────────
redis>=5.0.7
django-redis>=5.4.0
kafka-python>=2.0.2

# ─── Production Server ────────────────────────────────────────────────
gunicorn>=22.0.0

# ─── Media / Imaging ──────────────────────────────────────────────────
pillow>=10.4.0
requests>=2.31.0
google-api-python-client>=2.130.0

# ─── PDF Generation ──────────────────────────────────────────────────
# ReportLab is the cross-platform choice (no GTK / Cairo needed on Windows).
reportlab>=4.0.9

# ─── AI / RAG Stack ───────────────────────────────────────────────────
# `google-generativeai`: deprecated, being migrated out.
# Embeddings use `google-genai`. Text/vision use `groq`.
google-generativeai>=0.7.2
google-genai>=1.0
groq>=0.25.0
chromadb>=0.5.5

# ─── ML — Sentiment + Vision + Forecasting ────────────────────────────
# CPU wheels: pip also needs --extra-index-url https://download.pytorch.org/whl/cpu
# for torch==2.2.0+cpu / torchvision==0.17.0+cpu.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu
numpy>=1.26.4,<2
transformers==4.40.0
prophet>=1.1.5
