# ─── Core Web Framework ────────────────────────────────────────────────
Django>=5.0,<5.2
djangorestframework>=3.15.2
django-cors-headers>=4.4.0
django-environ>=0.11.2

# ─── Database ──────────────────────────────────────────────────────────
# mysqlclient ships pre-built wheels on Windows for CPython 3.12.
# If the wheel is unavailable, swap to `PyMySQL` and add a shim in manage.py.
mysqlclient>=2.2.4

# ─── Caching / Messaging ───────────────────────────────────────────────
redis>=5.0.7
django-redis>=5.4.0
kafka-python>=2.0.2

# ─── Production Server ────────────────────────────────────────────────
gunicorn>=22.0.0

# ─── Media / Imaging ──────────────────────────────────────────────────
pillow>=10.4.0

# ─── AI / RAG Stack ───────────────────────────────────────────────────
# `google-generativeai` is the package named in PRD Prompt 1A. It has been
# superseded by `google-genai`; keep this until Prompt 4A so the migration
# is a single, isolated change with its own tests.
google-generativeai>=0.7.2
chromadb>=0.5.5

# ─── ML — Sentiment + Forecasting ─────────────────────────────────────
# torch wheels from PyPI default to CPU on Windows (no CUDA).
torch>=2.2.2
transformers>=4.43.3
prophet>=1.1.5
