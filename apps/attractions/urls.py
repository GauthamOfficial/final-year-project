"""
Routes for the `attractions` app.

Populated in Prompt 2A (PRD Appendix) with `AttractionsViewSet`,
`DistrictsViewSet`, and `MediaAssetsViewSet` registered on a DRF
DefaultRouter. Empty for now so the project boots after Prompt 1A.
"""

from django.urls import path

app_name = "attractions"

urlpatterns: list[path] = []
